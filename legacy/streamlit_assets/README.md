# Legacy Streamlit Assets

These files are kept only for the temporary Streamlit version (`Mini_Game/app.py`).

- `background.jpg`
- `win.mp3`

Release policy:
- Not required by the web app in `Mini_Game/webapp`.
- Exclude from public release package.
- Remove after Streamlit legacy app is fully retired.
